- hosts: all
  user: pi
  sudo: true
  sudo_user: root

  tasks:
    - name: Install Packages
      apt: pkg={{ item }} state=latest update_cache=yes cache_valid_time=86400
        - nginx
        - golang
